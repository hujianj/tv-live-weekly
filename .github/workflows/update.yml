name: Verify and publish IPTV playlist

on:
  workflow_dispatch:
  schedule:
    # Weekly, Beijing time Monday 04:20. Full stream verification is network-heavy.
    - cron: "20 20 * * 0"
  push:
    paths:
      - "scripts/verify_sources.py"
      - "scripts/curate_ku9.py"
      - ".github/workflows/update.yml"

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Verify all configured IPTV sources
        run: python scripts/verify_sources.py
      - name: Curate Ku9 channel order
        run: python scripts/curate_ku9.py
      - name: Commit verified playlist
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: verify and publish playable IPTV playlist"
          file_pattern: live.txt live-verified.txt live-curated.txt ku9-live.txt live.m3u source-report.md check-report.md curated-report.md sources_status.csv stream_check_results.csv
      - name: Purge jsDelivr CDN
        if: always()
        run: |
          set -e
          for file in live-curated.txt live.txt ku9-live.txt live-verified.txt live.m3u curated-report.md source-report.md check-report.md; do
            echo "Purging $file"
            curl -fsS "https://purge.jsdelivr.net/gh/${{ github.repository }}/$file" || true
            curl -fsS "https://purge.jsdelivr.net/gh/${{ github.repository }}@main/$file" || true
          done
      - name: Smoke test primary CDN playlist freshness
        run: |
          python - <<'PY'
          import hashlib, time, urllib.request
          from pathlib import Path
          cdn_url = "https://cdn.jsdelivr.net/gh/${{ github.repository }}@main/live-curated.txt"
          expected = Path("live-curated.txt").read_bytes()
          expected_hash = hashlib.sha256(expected).hexdigest()
          expected_size = len(expected)
          def fetch(url):
              with urllib.request.urlopen(url + f"?smoke={int(time.time())}", timeout=30) as r:
                  return r.read()
          last_hash = ""
          last_size = 0
          for i in range(20):
              try:
                  cdn = fetch(cdn_url)
                  last_hash = hashlib.sha256(cdn).hexdigest()
                  last_size = len(cdn)
                  text = cdn.decode("utf-8", "replace")
                  assert last_hash == expected_hash, f"CDN hash {last_hash} != local hash {expected_hash}"
                  assert "央视频道,#genre#" in text
                  assert "影视剧场,#genre#" in text
                  assert "综合娱乐,#genre#" in text
                  assert "影视娱乐,#genre#" not in text
                  assert "其他频道,#genre#" not in text
                  print("CDN freshness smoke OK", last_size, last_hash)
                  break
              except Exception as e:
                  print("CDN freshness smoke retry", i + 1, repr(e), "last_size", last_size, "last_hash", last_hash, "expected_size", expected_size, "expected_hash", expected_hash)
                  time.sleep(15)
          else:
              raise SystemExit(f"CDN freshness smoke failed: last_size={last_size} last_hash={last_hash} expected_size={expected_size} expected_hash={expected_hash}")
          PY


